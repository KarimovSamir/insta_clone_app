import { ResourceType } from '../../../core/types/resource-type';

export type PostDataOutput = {
  title: string;
  shortDescription: string;
  content: string;
  blogId: string;
  blogName: string,
  createdAt: string,
};
